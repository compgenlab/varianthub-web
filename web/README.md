# VariantHub web front-end

The React app lives here. Not yet scaffolded — this is Chunk 7.

Build against the contract in [`../docs/api.md`](../docs/api.md) and the design
handoff in [`../design_handoff_varianthub/`](../design_handoff_varianthub/).
`VariantHub.dc.html` there is an interactive reference prototype, explicitly not
code to copy: recreate the design with real routing, a server-backed data layer,
a virtualized results table, and igv.js for the browser view.
