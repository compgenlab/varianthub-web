# build output
/varianthub-web
/bin/
/dist/

# local config + secrets
.env
varianthub-web.toml
*.local.yaml

# node / react
/web/node_modules/
/web/dist/
/web/.vite/

# local scratch
/tmp/

# go workspace (machine-local)
go.work
go.work.sum

# design handoff bundle (kept, but not the zip)
*.zip

# local compose overrides (real paths, real secrets)
deploy/compose/.env

# A local config file at the repo root; deploy/compose/varianthub-web.toml is
# the tracked example that the dev stack mounts.
/varianthub-web.toml
